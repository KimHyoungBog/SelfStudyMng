# Handoff — 중학생 학습관리 앱 (SelfStudyMng) — 2026-07-30 07:40

## 현재 상태
- 완료:
  - 초기 구현 (과목 설정 / 오늘의 학습 / 주간 요약, localStorage 저장, 반응형)
  - `/evaluate` 1차 (subagent 위임) → FAIL(blocker 1, major 3, minor 3) → 전량 수정 → 재검증 통과
  - 주간 요약에 "이전 주 / 다음 주 / 이번 주로" 이동 기능 추가 (과거 기록 조회)
  - `/evaluate` 2차 (subagent 위임, Phase 6 대상) → **PASS** (blocker 없음)
  - GitHub 원격을 `https://github.com/KimHyoungBog/SelfStudyMng.git`으로 설정, 전 커밋 push 완료
  - `.claude/settings.local.json`을 `.gitignore` 처리 + 저장소 추적 해제
- 검증됨:
  - `npx tsc --noEmit` 0 에러, `npm run lint` 0 에러(`.eslintrc.cjs` 신설)
  - `npm run test:e2e` 3/3 통과 (TC1 정상 흐름, TC4 과목 미등록, TC5 이전 주 이동)
  - 회의적 점검: 자정 경계, 과목 삭제 시 고아 레코드, 입력값 상한, 월/연도 경계 넘는 주 이동, 모바일 375px — 모두 실제 실행으로 확인
- 진행 중: 없음 (요청받은 작업 단위는 모두 완료·push됨)

## 다음 단계
- 즉시 할 일은 없음. 사용자가 새 기능/버그를 요청하면 `/plan` 또는 `/grill`부터 시작.
- 후속 검토 후보 (사용자 요청 시에만 착수, 지금은 미착수):
  1. **과거 주 목표값 소급 반영 문제** — `Subject.targetMinutes`에 시점 스냅샷이 없어, 과목 설정을 나중에 바꾸면 과거 주 조회 화면의 목표/달성률도 함께 바뀜. `DailyRecord`에 기록 당시 목표를 스냅샷으로 저장하는 데이터 모델 확장이 필요 (spec.md "명시된 가정"에 현재는 의도적 단순화로 문서화만 해둠).
  2. **과목 삭제 시 과거 기록 영구 삭제(soft-delete 없음)** — 과거 기록 조회 기능과 정책적으로 상충 가능. spec.md에 가정으로 명문화했으나, soft-delete 전환 여부는 사용자 판단 필요.
  3. 자정 경과 시 화면 자동 갱신 없음 (Out of Scope로 보류 중, `eval-checklist.md` 결함 #7)

## 핵심 결과물

### 생성된 파일
- `src/utils/date.ts` — 로컬 타임존 기준 `YYYY-MM-DD` 포맷 유틸 (`toLocalDateString`), 자정 경계 버그 수정의 핵심
- `.eslintrc.cjs` — ESLint 설정 (eslint 8 + @typescript-eslint 7 + react-hooks/react-refresh)
- `.claude/harness/handoff-2026-07-30-0740.md` — 이 문서

### 변경된 파일
- `src/pages/Today.tsx` — 날짜 키를 `toLocalDateString`로 교체, 실제 시간 입력에 720분 상한 클램프
- `src/pages/Weekly.tsx` — `getWeekDates`에 `weekOffset` 파라미터 추가, 주간 네비게이션 UI(이전/다음/이번 주로), 날짜 계산 로컬 타임존 통일
- `src/pages/Settings.tsx` — 과목 삭제 시 관련 `DailyRecord`도 함께 제거, 목표 시간 입력에 480분 상한 클램프
- `src/index.css` — `.week-nav` 관련 스타일 추가
- `test/study-app.spec.ts` — TC5(주간 이전/다음 이동) 추가
- `test/example.spec.ts` — 삭제 (미사용 Playwright 템플릿)
- `package.json` / `package-lock.json` — `lint` 스크립트, ESLint 관련 devDependencies 추가
- `.gitignore` — `.claude/settings.local.json` 추가
- `.claude/harness/spec.md`, `plan.md`, `eval-checklist.md` — 완료 조건 체크, Phase 6(주간 이력 조회) 추가, 평가 결과·가정 반영

## 결정 사항
- **날짜 키를 UTC(`toISOString`) 대신 로컬 타임존 기준으로 통일**: `/evaluate` 1차에서 blocker로 식별된 자정 경계 버그의 근본 원인. 사용자 확인 없이 Generator 단계에서 바로 수정(사용자가 "물어보지 말고 알아서 처리" 명시적 요청, 2026-07-29 세션 중).
- **ESLint 신규 도입(eslint 8.x, classic `.eslintrc.cjs`)**: plan.md 하드 임계값("ESLint 에러 0건")을 검증할 수단이 없어서 추가. Vite 5 + ESLint 9(flat config) 대신 8.x를 선택한 이유는 기존 vite-react-ts 템플릿과의 호환성 및 최소 변경.
- **과거 주 목표 소급 반영 문제를 코드 수정 대신 spec.md 가정으로 문서화**: `/evaluate` 2차에서 major로 식별됐으나, 해결하려면 `DailyRecord`에 목표 스냅샷을 추가하는 데이터 모델 확장이 필요해 이번 기능 요청("이전 날로 이동해 과거 기록 조회")의 스코프를 벗어난다고 판단. 사용자에게 재확인하지 않고 Generator가 자체 판단(ISO 25010 관점: 기능 적합성보다 범위 통제 우선, 침묵한 포기 대신 명시적 기록).
- **과목 삭제 시 과거 기록도 함께 삭제되는 기존 동작을 유지**: soft-delete로 바꾸지 않고 가정으로만 명문화. 이유는 위와 동일(스코프 통제).

## 미해결 이슈
- 알려진 버그: 없음 (2차 evaluate PASS, blocker 0건)
- 보류된 결정:
  - 과거 주 목표 시점 스냅샷 도입 여부 — 사용자 결정 필요 (위 "다음 단계" 1번)
  - 과목 삭제 정책을 soft-delete로 바꿀지 여부 — 사용자 결정 필요 (위 "다음 단계" 2번)
- 사용자 확인 대기: 없음

## 참조
- spec: `.claude/harness/spec.md`
- plan: `.claude/harness/plan.md`
- eval: `.claude/harness/eval-checklist.md` (1차 결함 수정 내역 + Phase 6 2차 평가 결과 포함)
- 원격: `https://github.com/KimHyoungBog/SelfStudyMng.git` (`master`, 최신 커밋 `2da23c2`)
